spring:
  application:
    name: ev-route-planner

  datasource:
    url: jdbc:postgresql://localhost:5432/evplanner
    username: evplanner
    password: evplanner

  flyway:
    enabled: true
    locations: classpath:db/migration

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

server:
  port: 8080